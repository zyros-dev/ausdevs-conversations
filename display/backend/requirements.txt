Flask>=3.0.0
Flask-CORS>=4.0.0
numpy>=1.26.0
pandas>=2.1.0
scikit-learn>=1.3.0

# GPU Acceleration (optional - for faster clustering computation)
# Install for CUDA 11: pip install cuml-cu11
# Install for CUDA 12: pip install cuml-cu12
# If not installed, will fall back to CPU-based clustering
# cuml>=23.0.0
